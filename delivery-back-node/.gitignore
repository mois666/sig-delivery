# Dependencies
/tuto.txt
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Production
/dist
/build
/out
/coverage
/.next/
/.vercel/
/.serverless/

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
.env.example
!.env.sample

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor-specific
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript specific
*.tsbuildinfo

# Prisma specific
# /prisma/migrations # Descomenta si no deseas rastrear las migraciones en git.
# Note: Keep schema.prisma in git, but ignore the generated client if it ends up in a custom location.
# However, usually the client is in node_modules, which is already ignored.

# OS specific
Thumbs.db
desktop.ini

# Optional: Ignore docker related files that shouldn't be shared
.docker-compose.db.data/
